---
export interface Props {
  id?: string;
  className?: string;
}

const { id, className } = Astro.props;
---

<section id={id} class={`mx-auto md:max-w-3xl lg:max-w-4xl pt-20 md:pt-28 ${className || ''}`}>
  <slot />
</section>
